dependencies {
    api project(':services:co-api')
    api project(':shared:base-api')

    // test
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}